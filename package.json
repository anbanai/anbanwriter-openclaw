{
  "name": "anban-creator-openclaw",
  "version": "2.7.3",
  "description": "WeChat & Seednote AI content creation suite for OpenClaw",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "openclaw": ">=2026.3.22"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.8.0"
  }
}
